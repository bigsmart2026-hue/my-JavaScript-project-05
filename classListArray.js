let classList = ["Big", "Smart", "Funds", "Victor", "Tochukwu", "Onuoha"];

function checkStudents() {
    for (let i = 0; i < classList.length; i++) {
        console.log("Student " + (i + 1) + ": " + classList[i]);
    }
}

checkStudents();